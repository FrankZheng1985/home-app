<!--pages/profile/points.wxml - 3D极简科技版-->
<view class="page-container">
  <!-- 头部区域 -->
  <view class="hero-section">
    <view class="hero-bg-glow"></view>
    <view class="hero-content page-fade-in">
      <view class="hero-title">积分明细</view>
      <view class="hero-subtitle">Your Points & Rewards</view>
    </view>
  </view>

  <!-- 积分概览卡片 (Glassmorphism Card) -->
  <view class="glass-card summary-card success-pop">
    <view class="summary-main">
      <text class="total-label">可用积分</text>
      <text class="total-value">{{summary.availablePoints || 0}}</text>
      <view class="redeem-btn btn-active-feedback" bindtap="showRedeemApplyModal" wx:if="{{summary.availablePoints > 0}}">申请兑现</view>
    </view>
    <view class="stat-row">
      <view class="stat-item">
        <text class="stat-label">本月获得</text>
        <text class="stat-value">+{{summary.thisMonth || 0}}</text>
      </view>
      <view class="stat-divider"></view>
      <view class="stat-item">
        <text class="stat-label">累计兑现</text>
        <text class="stat-value">-{{summary.redeemedTotal || 0}}</text>
      </view>
      <view class="stat-divider"></view>
      <view class="stat-item">
        <text class="stat-label">家庭排名</text>
        <text class="stat-value">{{summary.rank || '-'}}</text>
      </view>
    </view>
  </view>

  <!-- 管理员待审核提示 -->
  <view class="glass-card notice-card" wx:if="{{isAdmin && pendingRedeemCount > 0}}" bindtap="goToRedeemReview">
    <view class="notice-content">
      <text class="notice-icon">🔔</text>
      <text class="notice-text">{{pendingRedeemCount}} 个兑现申请待审核</text>
    </view>
    <text class="arrow">›</text>
  </view>

  <!-- 筛选标签 -->
  <view class="section-header">
    <text class="section-title">收支明细</text>
    <view class="filter-tabs">
      <view class="filter-item {{filterType === 'all' ? 'active' : ''}}" data-type="all" bindtap="switchFilter">全部</view>
      <view class="filter-item {{filterType === 'earn' ? 'active' : ''}}" data-type="earn" bindtap="switchFilter">获得</view>
      <view class="filter-item {{filterType === 'redeem' ? 'active' : ''}}" data-type="redeem" bindtap="switchFilter">兑现</view>
    </view>
  </view>

  <!-- 积分列表 -->
  <view class="transactions-list">
    <view class="transaction-item glass-card" wx:for="{{transactions}}" wx:key="id">
      <view class="trans-icon">{{item.type === 'earn' ? '📈' : (item.type === 'redeem' ? '🎁' : '📉')}}</view>
      <view class="trans-info">
        <text class="trans-title">{{item.description}}</text>
        <text class="trans-time">{{item.createdAtText}}</text>
      </view>
      <view class="trans-points {{item.type === 'earn' ? 'positive' : 'negative'}}">
        {{item.type === 'earn' ? '+' : '-'}}{{item.points}}
      </view>
    </view>
    
    <view class="empty-tip" wx:if="{{transactions.length === 0}}">暂无积分记录</view>
  </view>

  <view class="safe-bottom"></view>
</view>
