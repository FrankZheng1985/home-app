/**app.wxss - 3D 极简科技版**/

/* ================================ */
/*        全局基础设置               */
/* ================================ */

page {
  box-sizing: border-box;
  background: #F8F9FB;
  font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'SF Pro Display', 'Helvetica Neue', sans-serif;
  line-height: 1.6;
  color: #1A1A1A;
}

view, scroll-view, swiper, button, input, textarea, label, navigator, image {
  box-sizing: border-box;
}

page {
  /* 🔤 字体系统 - 响应式阶梯 */
  --font-size-xs: clamp(20rpx, 2.5vw, 24rpx);
  --font-size-sm: clamp(24rpx, 3vw, 28rpx);
  --font-size-md: clamp(28rpx, 3.5vw, 32rpx);
  --font-size-lg: clamp(32rpx, 4vw, 36rpx);
  --font-size-xl: clamp(40rpx, 5vw, 48rpx);
  --font-size-xxl: clamp(48rpx, 6vw, 56rpx);
  --font-size-huge: clamp(64rpx, 8vw, 72rpx);
  --font-size-giant: clamp(80rpx, 10vw, 96rpx);

  /* 📏 间距系统 - 响应式阶梯 */
  --space-xs: clamp(8rpx, 1vw, 12rpx);
  --space-sm: clamp(16rpx, 2vw, 20rpx);
  --space-md: clamp(24rpx, 3vw, 32rpx);
  --space-lg: clamp(32rpx, 4vw, 40rpx);
  --space-xl: clamp(48rpx, 6vw, 64rpx);
  --space-xxl: clamp(64rpx, 8vw, 80rpx);

  /* 📐 圆角系统 - 响应式阶梯 */
  --radius-sm: clamp(12rpx, 1.5vw, 16rpx);
  --radius-md: clamp(20rpx, 2.5vw, 24rpx);
  --radius-lg: clamp(28rpx, 3.5vw, 32rpx);
  --radius-xl: clamp(40rpx, 5vw, 48rpx);
  --radius-full: 999rpx;

  /* 🌫️ 毛玻璃效果定义 */
  --glass-bg: rgba(255, 255, 255, 0.7);
  --glass-blur: blur(40rpx);
  --glass-border: 1rpx solid rgba(255, 255, 255, 0.5);
  
  /* 📐 布局系统 */
  --safe-bottom-height: calc(160rpx + env(safe-area-inset-bottom));
  
  /* 🌑 阴影系统 - 柔和深邃 */
  --shadow-sm: 0 8rpx 24rpx rgba(0, 0, 0, 0.05);
  --shadow-md: 0 16rpx 48rpx rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 24rpx 64rpx rgba(0, 0, 0, 0.12);
  
  /* 🎨 极简配色 */
  --tech-black: #000000;
  --tech-white: #FFFFFF;
  --tech-grey: #F0F2F5;
  --text-main: #1A1A1A;
  --text-grey: #999999;
  --accent-blue: #4facfe;
  --accent-green: #43a047;
  --accent-red: #FF4D4F;
  --accent-gold: #FFD93D;
}

/* ================================ */
/*          通用布局组件             */
/* ================================ */

/* 全局页面容器 */
.page-container {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  padding: 0 var(--space-lg);
  box-sizing: border-box;
}

/* 玻璃拟态卡片 */
.glass-card {
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: var(--glass-border);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-sm);
  width: 100%;
}

/* 极简主按钮 */
.primary-btn {
  background: var(--tech-black);
  color: var(--tech-white);
  height: 110rpx;
  border-radius: 55rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-size-lg);
  font-weight: 600;
  box-shadow: 0 20rpx 40rpx rgba(0, 0, 0, 0.2);
}

.primary-btn.disabled {
  background: #EEE;
  color: #CCC;
  box-shadow: none;
}

/* 极简次按钮 */
.secondary-btn {
  background: var(--tech-grey);
  color: #666;
  height: 100rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50rpx;
  font-size: var(--font-size-md);
  font-weight: 600;
}

/* ================================ */
/*          动画效果                 */
/* ================================ */

/* 浮动动画 */
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20rpx); }
}

.animate-float {
  animation: float 5s ease-in-out infinite;
}

/* 弹出动画 */
@keyframes pop {
  0% { transform: scale(0.9); opacity: 0; }
  100% { transform: scale(1); opacity: 1; }
}

.success-pop {
  animation: pop 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

/* 点击反馈 */
.btn-active-feedback:active {
  transform: scale(0.96);
  opacity: 0.8;
}

/* ================================ */
/*          安全区域适配             */
/* ================================ */

.safe-bottom {
  height: var(--safe-bottom-height);
  width: 100%;
  flex-shrink: 0;
}

.safe-top {
  padding-top: env(safe-area-inset-top);
}
